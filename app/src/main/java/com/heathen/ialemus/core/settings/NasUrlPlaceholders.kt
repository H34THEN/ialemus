package com.heathen.ialemus.core.settings

/**
 * Hint text for URL fields only — never used as default persisted values.
 */
object NasUrlPlaceholders {
    const val BRIDGE = LocalServiceDefaults.BRIDGE_FUTURE
    const val METUBE = LocalServiceDefaults.METUBE
    const val SLSKD = LocalServiceDefaults.SLSKD
    const val NAS_UI = LocalServiceDefaults.NAS_UI
}
