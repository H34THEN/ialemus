package com.heathen.ialemus.core.settings

/**
 * Placeholder for DataStore preferences and encrypted token storage.
 *
 * TODO(MVP 2): Persist bridge URL and API token with EncryptedSharedPreferences or Keystore-backed storage.
 */
object SettingsPlaceholder {
    const val TOKEN_MASK = "••••••••"
}
