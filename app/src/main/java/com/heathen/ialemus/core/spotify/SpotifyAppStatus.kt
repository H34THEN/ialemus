package com.heathen.ialemus.core.spotify

enum class SpotifyAppStatus(val label: String) {
    INSTALLED("Spotify app installed"),
    NOT_INSTALLED("Spotify app not installed"),
    UNKNOWN("Unknown"),
}
